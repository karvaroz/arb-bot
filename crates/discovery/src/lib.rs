pub mod jupiter;

// raydium.rs / orca.rs / meteora.rs (per-DEX pool discovery, e.g. getProgramAccounts
// filtered by pool layout) land alongside their decoders in dex-raydium/dex-orca/
// dex-meteora — not stubbed here to avoid guessing account filters ahead of the
// real decoder work.
